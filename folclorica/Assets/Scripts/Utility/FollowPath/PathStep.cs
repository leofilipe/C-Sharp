using UnityEngine;
using System.Collections;

/// <summary>
/// Aids in controlling the steps of the storyboard
/// </summary>
public class PathStep : MonoBehaviour {
	
	public float duration;	//duration for this step
	public Transform step;	//the step of the path this frame corresponds to

	// Use this for initialization
	void Start () {
		//no need to doing anything
	}
	
	// Update is called once per frame
	void Update () {
		//no need to doing anything
	}
}
